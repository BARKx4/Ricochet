(( continue rechecks the condition; break exits the nearest loop. ))

0 count var
0 total var

$count 10 < while
  $count 1 + count set

  $count 3 = if
    continue
  end

  $count 6 = if
    break
  end

  $total $count + total set
end

$total println
