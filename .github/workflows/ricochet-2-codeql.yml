name: Ricochet 2 CodeQL

on:
  push:
    branches: ["ricochet-2"]
  pull_request:
    branches: ["ricochet-2"]
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

jobs:
  analyze:
    uses: ./.github/workflows/codeql.yml
